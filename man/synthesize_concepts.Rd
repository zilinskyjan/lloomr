% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthesize.R
\name{synthesize_concepts}
\alias{synthesize_concepts}
\title{Synthesize concepts from clustered examples}
\usage{
synthesize_concepts(
  cluster_df,
  text_col,
  id_col,
  chat,
  cluster_id_col = "cluster_id",
  n_concepts = NULL,
  batch_size = NULL,
  pattern_phrase = "unifying pattern",
  dedupe = TRUE,
  seed = NULL,
  prompt_template = NULL,
  max_active = 10,
  rpm = 500
)
}
\arguments{
\item{cluster_df}{Data frame with text, ID, and cluster columns
(the output of \code{\link[=cluster_texts]{cluster_texts()}}).}

\item{text_col, id_col}{Column names (strings) for text and document IDs.}

\item{chat}{An ellmer chat object (a capable model is recommended here;
upstream defaults to gpt-4o for this step).}

\item{cluster_id_col}{Name of the cluster column. Default \code{"cluster_id"}.}

\item{n_concepts}{Number of concepts to request per cluster. Default
\code{NULL} = \code{ceiling(cluster_size / 3)} (upstream heuristic).}

\item{batch_size}{Optional maximum examples per prompt; clusters larger
than this are split across several prompts. Default \code{NULL} (no split).}

\item{pattern_phrase}{Noun used in the prompt for what to find. Default
\code{"unifying pattern"} (upstream's session pipeline uses \code{"unique topic"}).}

\item{dedupe}{Drop concepts with identical name + prompt. Default \code{TRUE}.}

\item{seed}{Optional seed term steering synthesis; recorded in the
concepts' \code{seed} field.}

\item{prompt_template}{Optional custom template (validated).}

\item{max_active, rpm}{Concurrency controls passed to \code{\link[=ll_query]{ll_query()}}.}
}
\value{
A list with:
\itemize{
\item \code{concepts} — a \code{\link[=new_concepts]{new_concepts()}} tibble (one row per concept,
\code{active = FALSE});
\item \code{assignments} — a tibble linking concepts to their exemplar
documents: \code{id_col}, \code{text_col}, \code{concept_id}, \code{concept_name},
\code{concept_prompt}, \code{seed} (only exemplar IDs actually present in the
concept's cluster are kept, as upstream).
Token/cost usage is attached to the list as attribute \code{"usage"}.
}
}
\description{
For each cluster of texts (from \code{\link[=cluster_texts]{cluster_texts()}}), asks the LLM to
propose high-level patterns: a 2-4 word name, a 1-sentence inclusion
criterion ("prompt"), and the IDs of 1-2 best-exemplifying examples.
This is the step that turns groups of bullets into candidate concepts.
}
\examples{
\dontrun{
chat <- ellmer::chat_openai(model = "gpt-5.2", echo = "none")
clusters <- cluster_texts(bullets, "text", "post_id")
synth <- synthesize_concepts(clusters, "text", "post_id", chat)
synth$concepts
}
}
