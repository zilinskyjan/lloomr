% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distill.R
\name{distill_filter}
\alias{distill_filter}
\title{Distill documents to salient quotes}
\usage{
distill_filter(
  df,
  text_col,
  id_col,
  chat,
  n_quotes = 3,
  seed = NULL,
  prompt_template = NULL,
  max_active = 10,
  rpm = 500
)
}
\arguments{
\item{df}{Data frame of documents.}

\item{text_col, id_col}{Column names (strings) for document text and IDs.}

\item{chat}{An ellmer chat object used for the LLM calls.}

\item{n_quotes}{Number of quotes to extract per document. Default 3.}

\item{seed}{Optional seed term to steer extraction (e.g. "media distrust").}

\item{prompt_template}{Optional custom template; must contain the fields
required by \code{validate_prompt("distill_filter", ...)}.}

\item{max_active, rpm}{Concurrency controls passed to \code{\link[=ll_query]{ll_query()}}.}
}
\value{
A tibble with columns \code{id_col} and \code{text_col}, where \code{text_col}
now holds the extracted quotes (newline-separated), one row per
document. Documents whose query failed are dropped (with a warning).
Token/cost usage is attached as attribute \code{"usage"}.
}
\description{
For each document, asks the LLM to extract \code{n_quotes} quotes copied
verbatim from the text (the optional \code{seed} steers which quotes count as
relevant). This is the first, optional step of the LLooM pipeline,
typically used for long documents; short texts (e.g. social media posts)
can skip straight to \code{\link[=distill_summarize]{distill_summarize()}}.
}
\examples{
\dontrun{
chat <- ellmer::chat_openai(model = "gpt-5.4-nano", echo = "none")
quotes <- distill_filter(df, "text", "doc_id", chat, n_quotes = 2)
}
}
