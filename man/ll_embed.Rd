% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm.R
\name{ll_embed}
\alias{ll_embed}
\title{Embed texts with the OpenAI embeddings API}
\usage{
ll_embed(
  texts,
  model = "text-embedding-3-large",
  api_key = Sys.getenv("OPENAI_API_KEY"),
  batch_size = 2048,
  dimensions = NULL,
  base_url = "https://api.openai.com/v1"
)
}
\arguments{
\item{texts}{Character vector of texts to embed.}

\item{model}{OpenAI embedding model name. Default
\code{"text-embedding-3-large"} (the upstream default for clustering).}

\item{api_key}{API key; defaults to the \code{OPENAI_API_KEY} environment
variable.}

\item{batch_size}{Maximum texts per request. Default 2048 (upstream
default; also the OpenAI API maximum).}

\item{dimensions}{Optional reduced dimensionality (supported by
text-embedding-3 models).}

\item{base_url}{API base URL (override for Azure/compatible endpoints).}
}
\value{
A numeric matrix with \code{length(texts)} rows.
}
\description{
Replaces upstream \code{get_embeddings()} / \code{call_embed_fn()}. Newlines are
replaced with spaces (as upstream, since they can degrade embedding
quality) and requests are batched. Any operator that takes an \code{embed_fn}
argument accepts a drop-in replacement with this signature, so other
embedding providers (or precomputed embeddings) can be used.
}
\examples{
\dontrun{
emb <- ll_embed(c("politics and elections", "sports scores"),
                model = "text-embedding-3-small")
dim(emb)
}
}
