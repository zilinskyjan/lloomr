% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concepts.R
\name{new_concepts}
\alias{new_concepts}
\title{Create a tibble of concepts}
\usage{
new_concepts(
  name,
  prompt,
  example_ids = NULL,
  active = FALSE,
  summary = NA_character_,
  seed = NA_character_
)
}
\arguments{
\item{name}{Character vector of concept names.}

\item{prompt}{Character vector of concept inclusion-criteria prompts
(recycled rules: must match \code{length(name)}).}

\item{example_ids}{List of character vectors of representative document
IDs, one element per concept (or a single character vector if creating
one concept). Defaults to no examples.}

\item{active}{Logical; whether concepts start active. Default \code{FALSE}
(matching upstream, where concepts are activated by selection).}

\item{summary, seed}{Optional character vectors (default \code{NA}).}
}
\value{
A tibble with class \code{lloom_concepts}.
}
\description{
Constructor for the concept table used throughout lloomr. Vectorized over
\code{name} and \code{prompt}; a fresh unique \code{id} is generated for each concept.
}
\examples{
new_concepts(
  name = c("Economic Anxiety", "Distrust of Media"),
  prompt = c(
    "Does the text express concern about economic conditions?",
    "Does the text express distrust toward news media?"
  )
)
}
