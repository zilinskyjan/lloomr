% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review.R
\name{review_merge}
\alias{review_merge}
\title{Merge overlapping concepts}
\usage{
review_merge(concepts, chat, max_active = 10, rpm = 500)
}
\arguments{
\item{concepts}{A concept tibble (see \code{\link[=new_concepts]{new_concepts()}}).}

\item{chat}{An ellmer chat object.}

\item{max_active, rpm}{Concurrency controls passed to \code{\link[=ll_query]{ll_query()}}.}
}
\value{
A list: \code{concepts} (with merged rows replacing originals) and
\code{merged} (tibble with columns \code{original_1}, \code{original_2}, \code{merged_name},
\code{merged_id}; zero rows if nothing merged).
}
\description{
Asks the LLM for pairs of similar/overlapping concepts and replaces each
pair with a newly named merged concept (with the union of the originals'
exemplar IDs). Only pairs whose two originals both exist (and were not
already consumed by an earlier merge) are applied.
}
\examples{
\dontrun{
res <- review_merge(concepts, chat)
res$merged  # which pairs were combined, and into what
}
}
