% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm.R
\name{ll_query}
\alias{ll_query}
\title{Run many structured LLM queries concurrently}
\usage{
ll_query(chat, prompts, type, max_active = 10, rpm = 500)
}
\arguments{
\item{chat}{An ellmer chat object, e.g. \code{ellmer::chat_openai()}.}

\item{prompts}{Character vector or list of prompt strings.}

\item{type}{An ellmer type spec describing the response shape
(see \code{\link[=lloom_type]{lloom_type()}}).}

\item{max_active}{Maximum number of simultaneous requests. Default 10.}

\item{rpm}{Requests-per-minute throttle. Default 500.}
}
\value{
A list with one element per prompt: the parsed result (a list
matching \code{type}), or \code{NULL} if that query failed. Token/cost usage for
the call is attached as attribute \code{"usage"} (a data frame, or \code{NULL}
when unavailable).
}
\description{
The workhorse behind every LLooM operator (replaces upstream
\code{multi_query_gpt_wrapper()}). Sends all prompts concurrently through
\code{\link[ellmer:parallel_chat]{ellmer::parallel_chat_structured()}} with the given output schema and
returns one parsed result per prompt. Failed queries yield \code{NULL} (with a
warning) rather than aborting the batch, mirroring upstream behavior.
}
\examples{
\dontrun{
chat <- ellmer::chat_openai(model = "gpt-5.4-nano", echo = "none")
prompts <- sapply(c("The economy is improving.", "Media trust is falling."),
  function(ex) render_prompt(
    lloom_prompt("distill_summarize"),
    list(ex = ex, seeding_phrase = "", n_bullets = 2, n_words = "3-5")
  ))
res <- ll_query(chat, prompts, lloom_type("distill_summarize"))
str(res)
attr(res, "usage")  # tokens and dollars
}
}
