% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review.R
\name{review_concepts}
\alias{review_concepts}
\title{Review a concept set: remove, merge, and optionally select}
\usage{
review_concepts(
  concepts,
  chat,
  assignments = NULL,
  seed = NULL,
  max_concepts = NULL,
  max_active = 10,
  rpm = 500
)
}
\arguments{
\item{concepts}{A concept tibble (see \code{\link[=new_concepts]{new_concepts()}}).}

\item{chat}{An ellmer chat object.}

\item{assignments}{Optional assignments tibble from
\code{\link[=synthesize_concepts]{synthesize_concepts()}} to keep in sync.}

\item{seed}{Optional seed term; switches to the seeded variant prompt.}

\item{max_concepts}{Optional; if supplied, runs \code{\link[=review_select]{review_select()}} too.}

\item{max_active, rpm}{Concurrency controls passed to \code{\link[=ll_query]{ll_query()}}.}
}
\value{
A list: \code{concepts}, \code{assignments} (or \code{NULL}), \code{removed}
(character names), \code{merged} (tibble).
}
\description{
The full auto-review pass run after synthesis (upstream \code{review()}):
removes too-narrow/too-broad concepts (or, with \code{seed}, off-topic ones),
merges overlapping pairs, and — if \code{max_concepts} is given — activates
the best subset. If an \code{assignments} table from \code{\link[=synthesize_concepts]{synthesize_concepts()}}
is supplied, it is kept in sync (removed concepts dropped, merged
concepts relabeled).
}
\examples{
\dontrun{
synth <- synthesize_concepts(clusters, "text", "post_id", chat)
reviewed <- review_concepts(synth$concepts, chat,
                            assignments = synth$assignments,
                            max_concepts = 8)
reviewed$concepts
}
}
