% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save.R
\name{lloom_write}
\alias{lloom_write}
\title{Write all session results to a folder}
\usage{
lloom_write(sess, dir, prefix = NULL)
}
\arguments{
\item{sess}{A \code{\link[=lloom_session]{lloom_session()}} after \code{\link[=lloom_score]{lloom_score()}}.}

\item{dir}{Output directory (created if needed).}

\item{prefix}{Optional filename prefix (e.g. \code{"study1"} gives
\code{study1_scores_long.csv}).}
}
\value{
Invisibly, a named character vector of the written file paths.
}
\description{
One call to persist a finished analysis (after \code{\link[=lloom_score]{lloom_score()}}). Writes
to \code{dir}:
}
\details{
\itemize{
\item \code{scores_long.csv} — the full score table (\code{\link[=lloom_results]{lloom_results()}}): one row
per (document, concept) pair with scores, rationales, highlights.
\item \code{scores_wide.csv} — one row per document, one column per concept
(\code{\link[=scores_wide]{scores_wide()}}), ready to join onto your main dataset.
\item \code{concepts.csv} — the concept table (names, prompts, active flags).
\item \code{concept_summary.csv} — the per-concept evidence table
(\code{\link[=lloom_export]{lloom_export()}} with quotes collapsed into single cells).
\item \code{session.rds} — the entire session object; restore with \code{readRDS()}
(chat objects survive and keep working).
}

For just the scores, you never need this function — the score table is
a plain data frame: \code{readr::write_csv(lloom_results(sess), "scores.csv")}.
}
\examples{
\dontrun{
sess <- lloom_score(lloom_gen(lloom_session(df, "text", "doc_id")))
lloom_write(sess, "results/")

# Later, in a fresh R session:
sess <- readRDS("results/session.rds")
}
}
