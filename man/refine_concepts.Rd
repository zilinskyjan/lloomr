% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refine.R
\name{refine_concepts}
\alias{refine_concepts}
\title{Refine concepts by match prevalence}
\usage{
refine_concepts(
  score_df,
  concepts,
  threshold = 1,
  generic_threshold = 0.75,
  rare_threshold = 0.05
)
}
\arguments{
\item{score_df}{Output of \code{\link[=score_concepts]{score_concepts()}}.}

\item{concepts}{Concept tibble to refine.}

\item{threshold}{Minimum score counting as a match. Default 1 (upstream:
only "strongly agree").}

\item{generic_threshold}{Concepts matching at least this fraction of
documents are dropped as generic. Default 0.75 (upstream).}

\item{rare_threshold}{Concepts matching less than this fraction are
dropped as rare. Default 0.05 (upstream).}
}
\value{
A list: \code{concepts} (kept rows), \code{generic} and \code{rare} (character
vectors of dropped concept names).
}
\description{
Data-driven pruning after scoring: drops concepts that matched too many
documents (generic) or too few (rare).
}
\examples{
concepts <- new_concepts(c("Everything", "Niche"), c("Is text?", "About X?"))
# "Everything" matches all 10 docs (generic); "Niche" matches none (rare)
score_df <- data.frame(
  doc_id = rep(1:10, 2),
  concept_id = rep(concepts$id, each = 10),
  concept_name = rep(concepts$name, each = 10),
  score = rep(c(1, 0), each = 10)
)
refine_concepts(score_df, concepts)
}
