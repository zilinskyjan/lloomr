% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{cluster_texts}
\alias{cluster_texts}
\title{Cluster texts by semantic similarity}
\usage{
cluster_texts(
  df,
  text_col,
  id_col,
  embed_fn = NULL,
  embed_model = "text-embedding-3-large",
  embeddings = NULL,
  min_cluster_size = NULL,
  randomize = FALSE,
  batch_size = 20,
  seed = NULL
)
}
\arguments{
\item{df}{Data frame of texts (typically one row per bullet).}

\item{text_col, id_col}{Column names (strings) for text and document IDs.}

\item{embed_fn}{Function mapping a character vector to an embedding
matrix (one row per text). Defaults to \code{\link[=ll_embed]{ll_embed()}} with \code{embed_model}.
Supply your own to use a different provider.}

\item{embed_model}{Embedding model name passed to \code{\link[=ll_embed]{ll_embed()}} when
\code{embed_fn} is not supplied. Default \code{"text-embedding-3-large"}
(upstream default).}

\item{embeddings}{Optional precomputed embedding matrix (one row per row
of \code{df} \emph{after} empty-text filtering); skips the embedding call.}

\item{min_cluster_size}{Minimum HDBSCAN cluster size. Default \code{NULL} =
\code{max(3, floor(n/10))} (upstream heuristic).}

\item{randomize}{If \code{TRUE}, skip embeddings entirely and assign shuffled
texts to pseudo-clusters of \code{batch_size} (upstream's \code{randomize} mode,
an ablation/fallback).}

\item{batch_size}{Pseudo-cluster size for \code{randomize} mode. Default 20.}

\item{seed}{Optional integer; if supplied, clustering is reproducible
(sets the RNG seed and runs UMAP single-threaded).}
}
\value{
A tibble with columns \code{id_col}, \code{text_col}, and \code{cluster_id}
(integer; -1 = noise/outliers, clusters numbered from 0), sorted by
\code{cluster_id}. The embedding matrix is attached as attribute
\code{"embeddings"} (\code{NULL} in randomize mode).
}
\description{
Embeds each text (typically the bullets from \code{\link[=distill_summarize]{distill_summarize()}}),
reduces the embeddings to 5 dimensions with UMAP, and clusters with
HDBSCAN. Each cluster later becomes one batch of examples for concept
synthesis.
}
\examples{
df <- data.frame(id = 1:6, text = paste("bullet point", 1:6))

# randomize mode needs no embeddings (upstream's ablation/fallback)
cluster_texts(df, "text", "id", randomize = TRUE, batch_size = 3, seed = 1)

\dontrun{
# Real clustering: embeds via the OpenAI API, then UMAP + HDBSCAN
cluster_texts(bullets, "text", "post_id", seed = 42)
}
}
