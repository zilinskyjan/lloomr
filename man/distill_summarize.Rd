% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distill.R
\name{distill_summarize}
\alias{distill_summarize}
\title{Distill documents to bullet-point summaries}
\usage{
distill_summarize(
  df,
  text_col,
  id_col,
  chat,
  n_bullets = "2-4",
  n_words_per_bullet = "5-8",
  seed = NULL,
  prompt_template = NULL,
  max_active = 10,
  rpm = 500
)
}
\arguments{
\item{df}{Data frame of documents.}

\item{text_col, id_col}{Column names (strings) for document text and IDs.}

\item{chat}{An ellmer chat object used for the LLM calls.}

\item{n_bullets}{Number of bullets per document; a number or a range
string like \code{"2-4"} (upstream default).}

\item{n_words_per_bullet}{Length of each bullet; a number or range string
like \code{"5-8"} (upstream default).}

\item{seed}{Optional seed term to steer extraction (e.g. "media distrust").}

\item{prompt_template}{Optional custom template; must contain the fields
required by \code{validate_prompt("distill_filter", ...)}.}

\item{max_active, rpm}{Concurrency controls passed to \code{\link[=ll_query]{ll_query()}}.}
}
\value{
A tibble with columns \code{id_col} and \code{text_col}, \strong{one row per
bullet} (document IDs repeat). Documents whose query failed are
dropped (with a warning). Token/cost usage is attached as attribute
\code{"usage"}.
}
\description{
For each document (or its quotes from \code{\link[=distill_filter]{distill_filter()}}), asks the LLM
for \code{n_bullets} short bullet-point summaries. Bullets are the unit that
gets clustered and synthesized into concepts downstream.
}
\examples{
\dontrun{
chat <- ellmer::chat_openai(model = "gpt-5.4-nano", echo = "none")
bullets <- distill_summarize(df, "text", "doc_id", chat, n_bullets = "1-2",
                             seed = "media trust")
}
}
