% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis.R
\name{lloom_vis}
\alias{lloom_vis}
\title{Heatmap of concept matches by group}
\usage{
lloom_vis(
  sess,
  slice_col = NULL,
  norm_by = NULL,
  threshold = 1,
  max_slice_bins = 5,
  include_outlier = TRUE
)
}
\arguments{
\item{sess}{A \code{\link[=lloom_session]{lloom_session()}} after \code{\link[=lloom_score]{lloom_score()}}.}

\item{slice_col}{Optional column of the session's data frame to slice
by (e.g. a party or time variable).}

\item{norm_by}{\code{"slice"} (default when slicing: fraction of each group's
documents), \code{"concept"}, or \code{"none"} (raw counts).}

\item{threshold}{Minimum score counting as a match. Default 1.}

\item{max_slice_bins}{Maximum bins for numeric slice columns. Default 5.}

\item{include_outlier}{Include the "Outlier" row. Default \code{TRUE}.}
}
\value{
A ggplot object.
}
\description{
The lloomr replacement for upstream's interactive matrix widget: a
ggplot2 heatmap of concepts (rows) by document groups (columns), where
fill encodes how common each concept is in each group and each tile is
labeled with the match count. Returns a ggplot object for further
customization.
}
\examples{
\dontrun{
sess <- lloom_score(lloom_gen(lloom_session(df, "text", "doc_id")))
lloom_vis(sess, slice_col = "party")
# It is a regular ggplot object:
lloom_vis(sess) + ggplot2::labs(title = "My title")
}
}
