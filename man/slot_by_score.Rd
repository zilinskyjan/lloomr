% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign.R
\name{slot_by_score}
\alias{slot_by_score}
\title{Slot each document into one topic from existing scores}
\usage{
slot_by_score(score_df, id_col, threshold = 0.75, other_label = "Other")
}
\arguments{
\item{score_df}{Output of \code{\link[=score_concepts]{score_concepts()}}.}

\item{id_col}{Column name for document IDs.}

\item{threshold}{Minimum best score required to assign a topic; below it
the document is labeled \code{other_label}. Default 0.75 ("agree" or
stronger).}

\item{other_label}{Label for documents matching no concept. Default
\code{"Other"}.}
}
\value{
A tibble with one row per document: \code{id_col}, \code{text}, \code{topic},
\code{topic_score} (the winning score), \code{tie} (logical).
}
\description{
Deterministic single-label assignment derived from a multi-label
\code{\link[=score_concepts]{score_concepts()}} result: each document gets the concept on which it
scored highest. Costs no LLM calls. Ties and non-matches are handled
explicitly:
\itemize{
\item if the document's best score is below \code{threshold}, it is labeled
\code{other_label};
\item if several concepts tie for the best score, the first in \code{score_df}
order wins and \code{tie = TRUE} flags the ambiguity.
}
}
\examples{
score_df <- data.frame(
  doc_id = rep(c("1", "2", "3"), each = 2),
  text = rep(c("clear economy doc", "ambiguous doc", "off-topic doc"), each = 2),
  concept_name = rep(c("Economy", "Media"), 3),
  score = c(1, 0.25,   1, 1,   0.5, 0.25)
)
# Doc 1: Economy wins; doc 2: tie (flagged); doc 3: below threshold -> Other
slot_by_score(score_df, "doc_id")
}
