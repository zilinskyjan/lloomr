% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review.R
\name{review_select}
\alias{review_select}
\title{Select the best concepts (sets \code{active})}
\usage{
review_select(concepts, max_concepts, chat, max_active = 10, rpm = 500)
}
\arguments{
\item{concepts}{A concept tibble (see \code{\link[=new_concepts]{new_concepts()}}).}

\item{max_concepts}{Maximum number of concepts to activate.}

\item{chat}{An ellmer chat object.}

\item{max_active, rpm}{Concurrency controls passed to \code{\link[=ll_query]{ll_query()}}.}
}
\value{
The concept tibble with \code{active} updated.
}
\description{
Asks the LLM to pick at most \code{max_concepts} high-quality, non-overlapping
concepts, and marks those rows \code{active = TRUE} (everything else
\code{FALSE}). If the LLM selects nothing usable, a random sample is
activated instead, with a warning (mirroring upstream's fallback).
}
\examples{
\dontrun{
concepts <- review_select(concepts, max_concepts = 5, chat)
concepts[concepts$active, c("name", "prompt")]
}
}
