% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/session.R
\name{lloom_score}
\alias{lloom_score}
\title{Score documents against the session's active concepts}
\usage{
lloom_score(
  sess,
  batch_size = 1,
  get_highlights = TRUE,
  score_all = FALSE,
  df = NULL,
  verbose = TRUE
)
}
\arguments{
\item{sess}{A \code{\link[=lloom_session]{lloom_session()}} after \code{\link[=lloom_gen]{lloom_gen()}}.}

\item{batch_size}{Documents per LLM call. Default 1 (upstream session
default; raise for cheaper, slightly less reliable scoring).}

\item{get_highlights}{Request supporting quotes. Default \code{TRUE}
(upstream session default).}

\item{score_all}{Activate and score \emph{all} concepts. Default \code{FALSE}.}

\item{df}{Optional alternative document set to score.}

\item{verbose}{Announce steps. Default \code{TRUE}.}
}
\value{
The updated session (field \code{score_df}).
}
\description{
Runs \code{\link[=score_concepts]{score_concepts()}} for the session (upstream \code{score()}): active
concepts only, against the full dataset (or \code{df}).
}
\examples{
\dontrun{
sess <- lloom_score(sess)                  # active concepts, full data
sess <- lloom_score(sess, score_all = TRUE)  # every generated concept
}
}
