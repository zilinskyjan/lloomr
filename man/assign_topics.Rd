% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign.R
\name{assign_topics}
\alias{assign_topics}
\title{Assign each document to exactly one topic}
\usage{
assign_topics(
  df,
  text_col,
  id_col,
  topics,
  chat,
  allow_other = TRUE,
  other_label = "Other",
  batch_size = 5,
  max_active = 10,
  rpm = 500
)
}
\arguments{
\item{df}{Data frame of documents.}

\item{text_col, id_col}{Column names (strings) for document text and IDs.}

\item{topics}{The fixed topic set: either a concept tibble (names and
prompts are shown to the model) or a character vector of topic names.}

\item{chat}{An ellmer chat object.}

\item{allow_other}{If \code{TRUE} (default), adds an \code{other_label} option for
documents fitting none of the topics; with \code{FALSE} the model must
force every document into one of the topics.}

\item{other_label}{Label used for non-fitting documents. Default
\code{"Other"}.}

\item{batch_size}{Documents per LLM call. Default 5.}

\item{max_active, rpm}{Concurrency controls passed to \code{\link[=ll_query]{ll_query()}}.}
}
\value{
A tibble with one row per document: \code{id_col}, \code{text}, \code{topic},
\code{rationale}. Documents missing from LLM responses get \code{topic = NA}
(with a warning). Token/cost usage is attached as attribute
\code{"usage"}.
}
\description{
Forced-choice classification of documents into a fixed topic set
(typically the concepts kept after \code{\link[=review_concepts]{review_concepts()}} /
\code{\link[=refine_concepts]{refine_concepts()}}, possibly edited by the user). Unlike
\code{\link[=score_concepts]{score_concepts()}}, which rates every (document, concept) pair
independently (multi-label), this returns exactly one topic per
document. The topic label is constrained to the provided set via the
structured-output schema, so the model cannot invent labels.
}
\details{
This is an lloomr extension; the upstream Python package has no
single-label operator.
}
\examples{
\dontrun{
topics <- new_concepts(
  name = c("Vaccine Promotion", "Media Distrust"),
  prompt = c("Promotes vaccination?", "Expresses distrust of media?")
)
assignments <- assign_topics(df, "text", "post_id", topics, chat)
table(assignments$topic)
}
}
