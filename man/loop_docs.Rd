% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refine.R
\name{loop_docs}
\alias{loop_docs}
\title{Find documents needing another concept-induction iteration}
\usage{
loop_docs(
  score_df,
  text_col = "text",
  id_col,
  threshold = 1,
  generic_threshold = 0.5
)
}
\arguments{
\item{score_df}{Output of \code{\link[=score_concepts]{score_concepts()}}.}

\item{text_col, id_col}{Column names for document text and IDs (\code{text_col}
should name the text column in \code{score_df}, i.e. \code{"text"}).}

\item{threshold}{Minimum score counting as a match. Default 1.}

\item{generic_threshold}{Fraction of documents a concept must match to
count as generic for coverage purposes. Default 0.5 (upstream).}
}
\value{
A tibble of documents (\code{id_col}, \code{text_col}) to feed into the
next iteration, or \code{NULL} if iteration should stop (every document
would be included again, or none would).
}
\description{
Implements the LLooM loop operator: identifies documents that are either
not covered by any concept or covered only by generic concepts (those
matching at least half of all documents), and returns them for another
round of concept generation.
}
\examples{
# Doc 1 matches the concept; docs 2-3 are uncovered -> returned for
# another round
score_df <- data.frame(
  doc_id = c("1", "2", "3"),
  text = c("covered doc", "uncovered doc", "another uncovered"),
  concept_id = "c1", concept_name = "Concept",
  score = c(1, 0, 0)
)
loop_docs(score_df, "text", "doc_id")
}
