% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/session.R
\name{lloom_estimate_gen_cost}
\alias{lloom_estimate_gen_cost}
\title{Estimate the cost of concept generation}
\usage{
lloom_estimate_gen_cost(sess, params = NULL)
}
\arguments{
\item{sess}{A \code{\link[=lloom_session]{lloom_session()}}.}

\item{params}{Parameter list; \code{NULL} = auto-suggest.}
}
\value{
A tibble with one row per step: estimated input/output tokens
and dollars (\code{NA} when the model's price is unknown).
}
\description{
Pre-flight cost estimate for \code{\link[=lloom_gen]{lloom_gen()}} (mirrors upstream
\code{estimate_gen_cost()}, with token counts approximated as characters/4
and prices looked up live from the provider's model list where
available). Estimates are rough; treat as order-of-magnitude.
}
\examples{
\dontrun{
lloom_estimate_gen_cost(sess)  # before committing to lloom_gen()
}
}
