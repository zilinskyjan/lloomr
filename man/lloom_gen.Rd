% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/session.R
\name{lloom_gen}
\alias{lloom_gen}
\title{Generate concepts: distill, cluster, synthesize, review}
\usage{
lloom_gen(
  sess,
  seed = NULL,
  params = NULL,
  n_synth = 1,
  max_concepts = NULL,
  auto_review = TRUE,
  sample_n = NULL,
  verbose = TRUE
)
}
\arguments{
\item{sess}{A \code{\link[=lloom_session]{lloom_session()}}.}

\item{seed}{Optional seed term steering all generation steps.}

\item{params}{Parameter list as from \code{\link[=lloom_suggest_params]{lloom_suggest_params()}}; \code{NULL} =
auto-suggest (with a message).}

\item{n_synth}{Number of synthesize iterations; iterations after the
first re-cluster the previous round's concepts (upstream behavior).
Default 1.}

\item{max_concepts}{If supplied, \code{\link[=review_select]{review_select()}} activates the best
subset at the end.}

\item{auto_review}{Run remove + merge review after synthesis. Default
\code{TRUE}.}

\item{sample_n}{Optional: generate concepts from a random sample of this
many documents (scoring still uses the full data). lloomr extension.}

\item{verbose}{Announce steps. Default \code{TRUE}.}
}
\value{
The updated session (fields \code{df_filtered}, \code{df_bullets},
\code{clusters}, \code{concepts}, \code{assignments}, \code{params}, \code{history}).
}
\description{
Runs the inductive half of the pipeline (upstream \code{gen()}):
optionally filter to quotes, summarize to bullets, cluster, synthesize
concepts per cluster, and auto-review (remove + merge; with
\code{max_concepts}, also select). The quote-filtering step is skipped when
\code{params$filter_n_quotes <= 1} (typical for short texts).
}
\examples{
\dontrun{
# Generate concepts from a 200-doc sample, steered toward a topic,
# and activate the best 8
sess <- lloom_gen(sess, seed = "media trust", sample_n = 200,
                  max_concepts = 8)
sess$concepts
}
}
