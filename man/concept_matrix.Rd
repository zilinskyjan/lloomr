% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis.R
\name{concept_matrix}
\alias{concept_matrix}
\title{Build the concept x slice match matrix}
\usage{
concept_matrix(
  score_df,
  id_col,
  slice_df = NULL,
  slice_col = NULL,
  threshold = 1,
  norm_by = c("none", "slice", "concept"),
  max_slice_bins = 5,
  include_outlier = TRUE
)
}
\arguments{
\item{score_df}{Output of \code{\link[=score_concepts]{score_concepts()}}.}

\item{id_col}{Column name for document IDs.}

\item{slice_df}{Optional data frame with \code{id_col} and \code{slice_col},
supplying a metadata column to slice by (typically the original input
data).}

\item{slice_col}{Optional column in \code{slice_df} to group documents by.
Character/factor columns use their values; numeric columns are binned
into up to \code{max_slice_bins} quantile bins.}

\item{threshold}{Minimum score counting as a match. Default 1.}

\item{norm_by}{\code{"none"} (raw counts), \code{"slice"} (fraction of the slice's
documents), or \code{"concept"} (fraction of the concept's total matches).}

\item{max_slice_bins}{Maximum bins for numeric slice columns. Default 5.}

\item{include_outlier}{Include the "Outlier" row. Default \code{TRUE}.}
}
\value{
A tibble: \code{slice}, \code{concept}, \code{n} (match count), \code{value}
(normalized per \code{norm_by}), \code{slice_size} (documents in the slice).
}
\description{
The tidy data behind \code{\link[=lloom_vis]{lloom_vis()}} (replaces upstream \code{prep_vis_dfs()}):
for every group of documents (a slice of a metadata column, plus "All")
and every concept, the number of matching documents. A synthetic
"Outlier" concept counts documents matching no concept (as upstream).
}
\examples{
score_df <- data.frame(
  doc_id = rep(c("1", "2", "3", "4"), each = 2),
  concept_name = rep(c("Economy", "Media"), 4),
  score = c(1, 0,  1, 1,  0, 1,  0, 0)
)
meta <- data.frame(doc_id = as.character(1:4), party = c("D", "D", "R", "R"))
concept_matrix(score_df, "doc_id", slice_df = meta, slice_col = "party",
               norm_by = "slice")
}
