% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/session.R
\name{lloom_suggest_params}
\alias{lloom_suggest_params}
\title{Suggest concept-generation parameters}
\usage{
lloom_suggest_params(sess, target_n_concepts = 20)
}
\arguments{
\item{sess}{A \code{\link[=lloom_session]{lloom_session()}}.}

\item{target_n_concepts}{Desired total number of generated concepts.
Default 20 (upstream).}
}
\value{
A list: \code{filter_n_quotes}, \code{summ_n_bullets}, \code{synth_n_concepts}.
}
\description{
Heuristic parameter suggestion (mirrors upstream
\code{auto_suggest_parameters()}): quotes per document scale with the median
sentence count; bullets scale with quotes; concepts per cluster aim for
\code{target_n_concepts} overall across an assumed ~3 clusters.
}
\examples{
\dontrun{
params <- lloom_suggest_params(sess)
sess <- lloom_gen(sess, params = params)
}
}
